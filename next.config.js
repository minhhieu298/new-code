module.exports = {
  /* config options here */
  // reactStrictMode: true,
  output : "standalone",
  productionBrowserSourceMaps: false,
  // swcMinify: true, // Sử dụng SWC để minify code (nhanh và nhẹ hơn Terser)
  compress: true, // Bật nén Gzip
};

